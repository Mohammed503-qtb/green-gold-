#!/bin/sh
# ============================================================
# ذهب أخضر — نقطة إقلاع حاوية الإنتاج
# يزامن جداول قاعدة البيانات ثم يشغل الخادم
# ============================================================
set -e

echo "🟢 [green-gold] مزامنة جداول قاعدة البيانات..."
node node_modules/prisma/build/index.js db push --skip-generate || {
  echo "⚠️ [green-gold] تعذرت مزامنة الجداول (قد تكون هناك تغييرات تتطلب قرارًا). متابعة التشغيل..."
}

echo "🟢 [green-gold] تشغيل الخادم على المنفذ ${PORT:-3000}..."
exec node server.js
